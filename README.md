# Bibliothèque UAC - Application Desktop (Electron + Node.js + MySQL)

Contenu: backend (Express + Sequelize), frontend (React + Vite), electron wrapper.

Suivez les étapes dans `backend/README_BACKEND.md` pour configurer la base de données et lancer les services.

